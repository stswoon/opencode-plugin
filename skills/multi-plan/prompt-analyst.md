# Промпт для Task / analyst

Роль задана агентом `analyst` (регистрируется плагином). Подставь задачу. У subagent нет истории чата.

---

Задача пользователя:

{{USER_TASK}}

Сделай шаги своей роли: проработать задачу, обновить `docs/spec`, обновить `docs/plan` со статусами.

Любое **допущение** (число/поведение/контракт, которых нет в спеке или декомпиле) и любую **нестыковку** требований (
пункты задачи противоречат друг другу, спеке, декомпилу или фактам репо) не решай сам. Верни их оркестратору как
подробные вопросы: где (`файл:строка` источника), что там сейчас, что не сходится, что зависит от ответа, 2–4 варианта с
последствиями для игры/спеки/тестов (первый — рекомендованный, «(Recommended)», с обоснованием) и возможность
свободного уточнения. Спеку и план правь только в части, не зависящей от вопроса; остальное не трогай, пока
пользователь не ответит.

После нарезки выполни обязательный пасс параллельности из роли: граф жёстких зависимостей, волны при 4 слотах, узкое место, попытки расширить фронт (снять нежёсткую зависимость, развести эксклюзивный файл, дробление по папкам, контракт вперёд при **≥2** разблокированных) без потери самодостаточности стори. Результат — блок «Фронт поставки …» в `docs/plan/README.md`; в ответе назови максимальную ширину фронта и узкое место.

Если нужны критичные уточнения — верни вопросы, спеку не порть.
