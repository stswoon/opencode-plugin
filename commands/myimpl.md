---
description: Реализовать стори со статусом «не сделано» (Dev → QA gate → Lead).
---

Загрузи skill `myimpl` (инструмент `skill`, `name: "myimpl"`) и выполни его как оркестратор для задачи:

$ARGUMENTS
