---
description: Реализовать стори со статусом «не сделано» (Dev → QA gate → Lead).
---

Загрузи skill `multi-impl` (инструмент `skill`, `name: "multi-impl"`) и выполни его как оркестратор для задачи:

$ARGUMENTS
